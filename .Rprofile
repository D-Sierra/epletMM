# Verificar si devtools está instalado, si no, instalarlo
if (!requireNamespace("devtools", quietly = TRUE)) {
  install.packages("devtools")
}

# Cargar devtools
library(devtools)

# Instalar hlapro desde GitHub
devtools::install_github("usuario/hlapro")


require(magrittr)
require(dplyr)
require(tidyverse)
#require(hlapro)
require(readxl)
